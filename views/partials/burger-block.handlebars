
<div class="burger-div">
	<div class="row">
		<div class="col-sm-7 col-7">
			{{burger_name}}
		</div>
		<div class="col-sm-3 col-3">
			<button class="change-devour btn btn-outline-secondary rounded-3 mb-2 " data-id="{{id}}" data-newdevour="{{devoured}}">
				{{#if devoured}}Devour It!{{else}}Reorder it!{{/if}}
			</button>
		</div>
	</div>
</div>